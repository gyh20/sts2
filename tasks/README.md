# STS2 Bingo task library

Each task has an individual Markdown description in this folder. The machine-readable catalog is `catalog.json`; evaluator implementations live in `server.js` (`evalTask`).

Total tasks: 56
